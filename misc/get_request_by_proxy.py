import requests

# proxies = {
#   "http": "http://user:pass@foo.bar.zoo:8080",
#   "https": "http://user:pass@foo.bar.zoo:8080",
# }

#requests.get("http://example.org", proxies=proxies)
requests.get("https://dict.longdo.com/", proxies=proxies)
